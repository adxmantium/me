// reducers/me.js

const init = {
	activeNavItem: 'Me',
};

export default function(state = init, action) {

    switch(action.type) {

        default:
            return state;
            
    }

};